# Este archivo ya debería existir según el contexto, pero si no lo hace, se crea así:
extends Node

# Método para cargar una escena
func load_scene(scene_path: String):
	var scene = preload(scene_path)
	if scene:
		get_tree().change_scene_to_packed(scene)
	else:
		print("Error: No se pudo cargar la escena ", scene_path)
